using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProductMaker : MonoBehaviour
{
    public GameObject pops;

    // Start is called before the first frame update
    void Start()
    {
        //pops = GameObject.FindGameObjectWithTag("products");
        //pops.transform.position = new Vector3(7, 0.8f, 1.7f);
        //pops.transform.Rotate(0, 0, 180);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
